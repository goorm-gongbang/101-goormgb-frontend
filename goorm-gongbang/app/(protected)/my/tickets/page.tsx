/* ===========================
   티켓 목록 페이지 (껍데기)
   Route: /my/tickets
   - 경기 예정 티켓 / 예매 내역 목록 표시 예정

   [TODO] 구현 시 작업 목록
   1. 티켓 목록 API 연동 (GET /api/tickets)
   2. 필터/탭 (예정 티켓 / 지난 티켓)
   3. 각 티켓 카드 → /my/tickets/[id] 로 라우팅
   4. 빈 상태 UI (티켓 없음 안내)
=========================== */

export default function TicketsPage() {
  return <div />;
}
